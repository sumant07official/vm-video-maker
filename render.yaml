services:
  - type: web
    name: vm-video-maker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn vm_server:app --bind 0.0.0.0:$PORT --workers 1 --timeout 600
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
